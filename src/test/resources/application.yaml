server:
  port: 8091
  servlet:
    context-path: /store


spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: DietApp
      auto-index-creation: true #ensure indices created even if collection already exists
  output:
    ansi:
      enabled: ALWAYS